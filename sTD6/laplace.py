# -*- coding: utf-8 -*-

import numpy as np
def laplace(p=3):
    a=np.zeros(nb_ligne, nb_colonne)
      np.diag(vecteur, numero de diag + -)
      np.ones(taille)
      *?
    b=a.reshope(nb_ligne*nb_colonne,1)
    b.flatten()[:,-1]
    v3=v1*v2.T
    c2=c1.reshape(1,6) [[1 2 3 4 5 6]]
    c1=v3.flatten()  [1 2 3 4 5 6]
    
    Lx=b     
    X=np.linalg.solve(L,b)
    X=L^(-1)*b
     =np.linalg.inv(L)*b
     
     np.dot(laplacien(p),no.ones)
    
if __name__="__main__"